{
	"name": "aibreze-site",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"docs:build": "node scripts/build-docs.mjs",
		"dev": "node ../scripts/sync-skill-static.mjs && pnpm docs:build && node ../scripts/ensure-lease.mjs aibreze-site 5181 && filepress dev --host 0.0.0.0",
		"build": "node ../scripts/sync-skill-static.mjs && pnpm docs:build && filepress build",
		"preview": "filepress preview",
		"check": "filepress check",
		"ship": "pnpm build && wrangler pages deploy build --project-name=aibreze",
		"deploy": "pnpm ship"
	},
	"devDependencies": {
		"getfilepress": "^0.1.8",
		"wrangler": "^4.28.0"
	},
	"pnpm": {
		"onlyBuiltDependencies": [
			"esbuild",
			"getfilepress",
			"workerd"
		]
	}
}
